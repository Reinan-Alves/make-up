/*const SelectFilterBrand = document.querySelector("#filter-brand"); //capture select brand
let createOption = () => {
  var option = `<Option value ="" >innertext</Option>`; //creat options
  return option;
};
SelectFilterBrand.appendChild(createOption); //appnend child

function mostraAlerta(elemento) {
  window.alert(elemento.value);
}


var products = document.querySelectorAll(".product");







  
    for (var i = 0; i < products.length; i++) {
      var product = products[i];


      var expression = new RegExp(this.value, "i");
      console.log(expression);
      if (!expression.test(name)) {
        product.classList.add("invisible");
      } else {
        product.classList.remove("invisible");
      }
    }
  } else {
    for (var i = 0; i < products.length; i++) {
      var product = products[i];
      product.classList.remove("invisible");
    }
  }
});*/
